{
  "name": "@limitedverse/template-snap-monorepo-metamask",
  "version": "0.1.0",
  "private": true,
  "description": "MetaMask Snap monorepo template",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "yarn workspaces foreach -Ap run start",
    "build": "yarn workspaces foreach -Ap run build",
    "clean": "yarn workspaces foreach -Ap run clean"
  },
  "packageManager": "yarn@3.6.4",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
